# Copie para .env.deploy e preencha. Nunca commite .env.deploy.

# --- Deploy apenas com token (API Hostinger) ---
# Token: hpanel.hostinger.com → Perfil → API
DEPLOY_TOKEN=
# ou HOSTINGER_API_TOKEN=

# ID da VPS (painel Hostinger: URL da VPS contém o ID; ex.: .../vps/1338576/overview)
VPS_VM_ID=1338576
# URL do repositório GitHub (público) com docker-compose.yml na raiz
REPO_URL=https://github.com/USUARIO/REPO

# --- Deploy por SSH/SCP (senha ou chave) ---
TARGET_HOST=76.13.232.76
TARGET_USER=root
TARGET_PATH=/var/www/app
# Para usar chave em vez de senha: SSH_PRIVATE_KEY_PATH=C:\Users\...\.ssh\id_ed25519
